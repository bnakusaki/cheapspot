import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
        fontSize: 16,
        colorPrimary: '#52c41a',
    },
    components: {
        Button: {
            primaryColor: '#000'
        }
    }
};

export default theme;