export default function LoadingHomePage() {
    return (
        <main className="h-screen flex items-center justify-center">
            <div className="loading-indicator" aria-label="Loading: A spinning indicator signifying the home page is loading"></div>
        </main>
    );
}

